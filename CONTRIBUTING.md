# 贡献指南

欢迎 Fork、提 Issue 和提交 PR。可以从一处错字、一个术语或一条链接开始。

## 报告问题

先搜索已有 Issue，避免重复。请提供章节文件与小节、原文片段、问题说明和建议；涉及公式时保留 LaTeX、符号定义和参考来源。PDF 问题请注明下载来源、版本与页码，构建问题请附系统、工具版本、命令与错误日志。

## 提交修改

1. Fork 仓库，从 `main` 创建分支，如 `fix/chapter-13-terminology`。
2. 修改 `chapters/` 下的 Markdown；图片放到 `images/`，文献放到 `chapters/bib.bib`。
3. 一个 PR 聚焦一个问题；避免夹带全文件重排或无关格式化。
4. 在 PR 中写清修改原因、对应 Issue、原文依据与验证结果。

纯文字纠错可以直接通过 GitHub 编辑，无需安装全套构建工具。涉及公式、引用、模板或构建脚本时，请按 [构建指南](docs/BUILDING.md) 运行 `make html epub` 与 `make check`。涉及 PDF 排版时请另行生成 PDF 检查。

请勿提交本地 `build/`、工具安装包、日志或密钥。预构建书籍的更新由维护者按发布流程统一处理。

## 内容与术语

- 忠实于原文；新增解释标记为「译者注」，附可核查来源。
- 首次出现的术语给出中文、英文和缩写，此后保持一致。
- CAI 指 **Constitutional AI（宪法 AI）**；SFT 为监督微调，DPO 为直接偏好优化。
- 保留变量、下标、公式编号和引用 key；不要仅为文字流畅改变数学含义。
- 图表或公式交叉引用写成 `[@fig:identifier]`、`[@eq:identifier]`，避免相邻汉字被解析进引用 key。
- 图片保留作者署名与来源；引入第三方材料时注明许可。
- AI 辅助修改也需要人工核实术语、公式和文献，不提交未经核查的大批改写。

## 审阅与合并

维护者会核查内容依据、修改范围和相关构建结果；自动检查通过不等于翻译已完成学术审校。贡献者可以在 PR 中说明不确定之处，无需为了提交而声称全部验证通过。

贡献适用本仓库已有的内容与代码许可。感谢每一位参与阅读、纠错和改进的朋友。
